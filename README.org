#+HTML_HEAD: <link rel="stylesheet" type="text/css" href="http://moonwwdz.win/emacs-html.css" />

* 日用emacs配置

该配置是学习[[https://github.com/emacs-china/Spacemacs-rocks][21 天学习 Emacs]]的产物，感觉日用也顺手，抄了些python的配置，感觉不错

** 主要特性

- 补全栈：=vertico=（纵向 UI）+ =consult=（命令）+ =orderless=（模糊匹配）+ =marginalia=（注解），=savehist= 按历史排序
- 编程语言：Go / Rust / Python / JS 经 =lsp-bridge= 提供补全、诊断、跳转、inlay hint
- =treesit-auto= 为非 lsp-bridge 语言（json/yaml/toml 等）自动启用 tree-sitter；Python/Go/Rust 刻意排除，继续走 lsp-bridge
- 导航与编辑：=avy=（屏内跳转）、=vundo=（可视化撤销树）、=wgrep=（grep 结果批量编辑）、=hl-todo=（关键字高亮）、=rainbow-mode=（颜色码色块）
- Evil（Vim 键位）、Rime 输入法、org-mode + org-modern、magit、dired 侧边栏

** 安装
#+BEGIN_SRC sh
  # macOS 需要安装 Python 3.10+（系统自带 3.9 不够）
  brew install python@3.12
  ln -s python3.12 /opt/homebrew/bin/python3

  # Python 依赖（lsp-bridge 运行所需）
  pip3 install epc orjson sexpdata six setuptools paramiko rapidfuzz watchdog packaging pyyaml --break-system-packages

  # 克隆配置
  git clone https://github.com/moonwwdz/emacsd.git ~/.emacs.d
  cd ~/.emacs.d
  git submodule init
  git submodule update
#+END_SRC

** 语言环境依赖

*** Go
#+BEGIN_SRC sh
  go install golang.org/x/tools/gopls@latest         # LSP 语言服务器
  go install golang.org/x/tools/cmd/goimports@latest  # 自动导入管理
  go install github.com/go-delve/delve/cmd/dlv@latest # 调试器
#+END_SRC

*** Rust
#+BEGIN_SRC sh
  rustup component add rust-src       # 源码（IDE 支持）
  rustup component add rustfmt        # 格式化
  rustup component add clippy         # 代码检查
#+END_SRC

*** Python
#+BEGIN_SRC sh
  pip3 install basedpyright           # LSP 语言服务器
  pip3 install ipython                # 交互式解释器
  pip3 install pytest                 # 测试框架
  pip3 install uv                     # 虚拟环境管理
#+END_SRC

** 全局快捷键

*** 搜索与导航
| keyboard | action              |
|----------+---------------------|
| C-s      | consult-line 搜索   |
| M-x      | 执行命令 (vertico)  |
| C-x C-f  | 查找文件            |
| C-x b    | consult-buffer 切换 |
| C-x C-r  | 最近打开的文件      |
| C-x C-b  | ibuffer             |
| M-s o    | occur 搜索并展示    |
| M-s i    | consult-imenu       |
| M-s g    | consult-ripgrep 全项目搜索 |
| C-'      | avy 跳转到屏内字符   |
| M-s j    | avy 跳转到行        |
| M-1/2/3  | 跳转到对应窗口      |

*** 编辑
| keyboard | action              |
|----------+---------------------|
| C-=      | expand-region 选区  |
| M-s e    | iedit 多点编辑      |
| C-x u    | vundo 可视化撤销树  |
| M-%      | 查找替换            |
| C-x r t  | 行首插入字符        |
| C-x r k  | 删除矩形区域        |
| C-\      | 切换输入法          |

*** 书签
| keyboard | action   |
|----------+----------|
| C-x r m  | 标记书签 |
| C-x r l  | 列出书签 |

*** 文件与窗口
| keyboard | action           |
|----------+------------------|
| C-x C-n  | 打开侧边栏       |
| C-x t 2  | 新建标签页        |
| C-x t r  | 重命名标签页      |
| C-x t 1  | 关闭其他标签页    |

*** Org-mode
| keyboard  | action       |
|-----------+--------------|
| C-c c     | Capture      |
| C-c a     | Agenda       |
| C-c C-l   | 编辑链接     |
| C-c C-e h | 导出 HTML    |

*** 工具
| keyboard | action                |
|----------+-----------------------|
| C-c g    | magit 状态            |
| C-c d    | macOS 词典查词         |
| C-c y    | 有道词典查词            |
| M-s s    | 英语自动补全 开关       |
| M-s p    | 粘贴辅助 开关           |
| M-s w    | 插入当前周数            |
| M-s t    | 切换主题               |
| C-c t i  | 切换 Web 缩进 (2/4空格) |

** LSP (lsp-bridge)

以下快捷键在 lsp-bridge-mode 激活时生效（go-mode、rust-mode、python-mode、emacs-lisp-mode 等）：

| keyboard | action            |
|----------+-------------------|
| M-.      | 跳转到定义         |
| M-,      | 返回跳转前位置      |
| C-c n    | 跳到下一个诊断      |
| C-c p    | 跳到上一个诊断      |
| C-.      | 显示文档           |
| M-n      | 补全菜单下一个候选项 |
| M-p      | 补全菜单上一个候选项 |

** 语言快捷键

*** Rust (rust-mode)
| keyboard    | action                    |
|-------------+---------------------------|
| C-c C-c     | 运行 (src/bin 下 cargo run --bin，否则 cargo run / rustc) |
| C-u C-c C-c | 运行并输入参数            |
| C-c C-b     | 构建 (cargo build)        |
| C-c C-t     | 测试 (cargo test)         |
| C-c C-k     | 检查 (cargo check)        |

*** Go (go-mode)
| keyboard    | action              |
|-------------+---------------------|
| C-c C-c     | 运行 (go run)       |
| C-u C-c C-c | 运行并输入参数      |
| C-c C-b     | 构建 (go build)     |
| C-c C-t     | 测试 (go test)      |
| C-c C-k     | 检查 (go vet)       |

*** Python (python-mode)
| keyboard    | action               |
|-------------+----------------------|
| C-c C-c     | 运行 (python3)       |
| C-u C-c C-c | 运行并输入参数         |
| C-c C-t     | 测试 (pytest)        |
| C-c C-k     | 语法检查 (py_compile) |
| <f5>        | 快速执行当前文件       |

** uv (Python 虚拟环境)
打开 Python 文件时自动激活项目 .venv，也可手动执行：
| command               | action           |
|-----------------------+------------------|
| M-x uv-activate       | 手动激活项目 .venv |
| M-x pyvenv-deactivate | 退出虚拟环境       |

** magit
| keyboard | action          |
|----------+-----------------|
| C-c g    | 激活 magit      |
| s        | stage (git add) |
| u        | unstage         |
| c c      | 进入提交页面      |
| C-c C-c  | 确认提交         |
| l l      | 查看日志         |
| P        | 推送            |
| 1,2,3,4  | 收缩/展开显示     |
